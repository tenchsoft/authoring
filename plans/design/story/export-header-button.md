# Design: export-header-button

## 한 줄 정의
헤더 액션 바의 Export 버튼. 클릭 시 Export Story 모달 열기.

## Component breakdown
| Component | role | debug_id |
|-----------|------|----------|
| Export button | `Button` | `story.header.export` |

## Visual properties
모두 기존 컴포넌트 재사용. 별도 visual properties 불필요.

## States
기존 헤더 버튼 상태(default, hover, active, disabled) 사용.
