# Design: open-header-button

## 한 줄 정의
헤더 액션 바의 Open 버튼. 클릭 시 네이티브 파일 열기 다이얼로그 호출.

## Component breakdown
| Component | role | debug_id |
|-----------|------|----------|
| Open button | `Button` | `story.header.open` |

## Visual properties
모두 기존 컴포넌트 재사용. 별도 visual properties 불필요.

## States
기존 헤더 버튼 상태(default, hover, active, disabled) 사용.
