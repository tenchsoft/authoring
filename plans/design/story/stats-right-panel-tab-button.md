# Design: stats-right-panel-tab-button

## 한 줄 정의
우측 보조 패널 탭 바의 Stats 탭 버튼. 클릭 시 통계 행 표시.

## Component breakdown
| Component | role | debug_id |
|-----------|------|----------|
| Stats tab button | `Button` | `story.tab.stats` |

## Visual properties
모두 기존 컴포넌트 재사용. 별도 visual properties 불필요.

## States
기존 탭 버튼 상태(default, hover, active, disabled) 사용.
