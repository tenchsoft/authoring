# Design: character-search-field

## 한 줄 정의
좌측 패널 상단의 캐릭터 검색 입력 필드. 기존 텍스트 입력 컴포넌트 재사용.

## Component breakdown
| Component | role | debug_id |
|-----------|------|----------|
| Search field | `TextInput` | `universe.character_search` |

기존 입력 필드 디자인 사용.

## Out of scope
- 검색 결과 필터링 (별도 background).
