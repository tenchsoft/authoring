# Design: template-picker

## 한 줄 정의
캐릭터 템플릿 선택 버튼. 기존 버튼 컴포넌트 재사용.

## Component breakdown
| Component | role | debug_id |
|-----------|------|----------|
| Template picker button | `Button` | `universe.template_picker` |

기존 버튼 디자인 사용.

## Out of scope
- 템플릿 목록 모달 (별도 design).
