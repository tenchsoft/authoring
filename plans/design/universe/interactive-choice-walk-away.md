# Design: interactive-choice-walk-away

## 한 줄 정의
대화형 모드의 "돌아가기" 선택지 버튼. 기존 버튼 컴포넌트 재사용.

## Component breakdown
| Component | role | debug_id |
|-----------|------|----------|
| Walk away button | `Button` | `universe.interactive.walk_away` |

기존 버튼 디자인 사용.

## Out of scope
- 선택지 액션 실행 (별도 spec).
