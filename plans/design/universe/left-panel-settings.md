# Design: left-panel-settings

## 한 줄 정의
좌측 패널 하단의 설정 버튼. 기존 버튼 컴포넌트 재사용.

## Component breakdown
| Component | role | debug_id |
|-----------|------|----------|
| Left panel settings | `Button` | `universe.left_panel.settings` |

기존 버튼 디자인 사용.

## Out of scope
- 설정 패널 (별도 design).
