# Design: interactive-choice-open-gate

## 한 줄 정의
대화형 모드의 "문 열기" 선택지 버튼. 기존 버튼 컴포넌트 재사용.

## Component breakdown
| Component | role | debug_id |
|-----------|------|----------|
| Open gate button | `Button` | `universe.interactive.open_gate` |

기존 버튼 디자인 사용.

## Out of scope
- 선택지 액션 실행 (별도 spec).
